<h2 class="mt-3">New book</h2>
<%= form_for(Book.new) do |f| %>
<label>Title</label>
<div class="row">
<%= f.text_field :title %>
</div>
<label>Opinion</label>
<div class="row">
<%= f.text_area :body %>
</div>
<%= f.submit 'Create Book', class:"btn btn-success"%>
<% end %>